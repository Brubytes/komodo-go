services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    environment:
      - DEMO_MODE=true
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

volumes:
  redis-data:
